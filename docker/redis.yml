version: "3.2"

services:
  frontend:
    stdin_open: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    build:
      context: ./../frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./../frontend:/home/node/code
